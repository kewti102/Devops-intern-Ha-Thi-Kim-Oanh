apiVersion: v1
kind: Pod
metadata:
  name: vpa-load-generator
  namespace: demo
spec:
  containers:
    - name: busybox
      image: busybox:1.36
      command:
        - /bin/sh
        - -c
        - "while true; do wget -q -O- http://demo-app-vpa.demo.svc.cluster.local; sleep 0.1; done"
  restartPolicy: Never
